# in-concert environment variables

# MongoDB connection string
MONGO_URL=mongodb://localhost:27017/in-concert

# BPM engine database name (definitions, instances, tasks, etc.)
MONGO_BPM_DB=in-concert

# Conversations database name
MONGO_DB=in-concert

# Server port
PORT=3000
